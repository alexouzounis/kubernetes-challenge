apiVersion: v1
kind: Service
metadata:
  name: {{ template "hello-unknown.fullname" . }}
  labels:
    app: {{ template "hello-unknown.name" . }}
    chart: {{ template "hello-unknown.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "hello-unknown.name" . }}
    release: {{ .Release.Name }}
